{{#each students}}
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{this.name}}</h5>
    <p class="card-text">{{this.class}}</p>
    <a href="/student/{{this.slug}}" class="btn btn-primary">Xem Điểm</a>
  </div>
</div>
{{/each}}